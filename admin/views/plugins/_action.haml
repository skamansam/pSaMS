.panel.panel-default{id: plugin.method_name }
  .panel-heading
    = tag_icon(:cogs)
    #{plugin.method_name} (action
    %em #{plugin.hook_name}
    )
    .action-links.pull-right
      = partial 'plugins/action_links', locals: {plugin: plugin}

  .panel-body
    %pre{class: 'language-ruby'}
      %code{class: 'language-ruby'}
        = preserve @plugin_object.new.method(plugin.method_name.to_sym).comment
        = preserve @plugin_object.new.method(plugin.method_name.to_sym).source
