%ul.nav.nav-tabs
  %li=link_to tag_icon(:list, pat(:list)), url(:plugins, :index)
  %li.active=link_to tag_icon(:plus, pat(:new)), url(:plugins, :new)

.tabs-content
  = form_for :plugin, url(:plugins, :create), :class => 'form-horizontal' do |f|
    =partial 'plugins/form', :locals => { :f => f }
